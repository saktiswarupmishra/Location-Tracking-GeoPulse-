# GeoPulse Backend Dependencies
# ==============================

# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
gunicorn>=22.0.0

# Database - MongoDB (native async - replaces deprecated Motor)
pymongo[srv]>=4.9.0

# Cache / Pub-Sub - Redis (async)
redis[hiredis]>=5.0.0

# Authentication
pyjwt[crypto]>=2.8.0
passlib[bcrypt]>=1.7.4

# Validation & Settings
pydantic>=2.7.0
pydantic-settings>=2.3.0

# OTP Provider
twilio>=9.0.0

# Middleware
slowapi>=0.1.9
python-multipart>=0.0.9

# Request IDs
python-ulid>=3.0.0

# HTTP Client (for testing & external calls)
httpx>=0.27.0

# Environment
python-dotenv>=1.0.0

# Hashing (for token family IDs)
hashlib-compat>=1.0.0; python_version<"3.0"

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
